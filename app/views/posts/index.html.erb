<h1><%= @header_text %></h1>

<%= link_to "Hot", { action: 'index', order: 'hot' } %>
<%= link_to "New", { action: 'index', order: 'new' } %>
<%= link_to 'New Post', new_post_path %>


<h3>Success Posts (3+ members)</h3>

<%= render 'post', posts: @success_posts %>

<hr>

<h3>Rising Posts</h3>

<%= render 'post', posts: @other_posts %>

<%= will_paginate @other_posts %>

